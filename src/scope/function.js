// function/Local Scope 

function greeting () {
    let userName = "Ana"

    if (userName === "Ana") {
        console.log(`Helloooo ${userName}`)
    } else {
        console.log (`No eres ${userName}`)
    }
}

greeting();
